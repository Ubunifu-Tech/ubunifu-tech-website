# Server-only Resend credential used by POST /api/contact.
# Copy this file to .env.local and replace the placeholder. Never expose this
# value through a NEXT_PUBLIC_ variable or commit a real key.
RESEND_API_KEY=re_replace_with_your_resend_api_key
